/* 
 * TEMA: Racing Red (Estilo F1)
 * 
 * Inspirado en la Fórmula 1 con rojo vibrante y negro profundo.
 * Agresivo, deportivo y lleno de energía, perfecto para portafolios
 * que quieren transmitir velocidad, pasión y determinación.
 * 
 * Consejo: El rojo es un color que llama la atención y transmite energía.
 * Ideal para portafolios creativos, gaming, deportes o proyectos audaces.
 */

export const racingRedTheme = {
  // Fondos principales
  primary: '#0a0a0a',              // Negro profundo como fondo principal
  'primary-dark': '#000000',       // Negro absoluto para contraste máximo
  
  // Colores de acento (rojo F1 vibrante)
  accent: '#dc0000',               // Rojo F1 icónico (Ferrari red)
  'accent-dark': '#a00000',        // Rojo oscuro para hover
  'accent-light': '#ff1e1e',       // Rojo brillante para destacados
  
  // Colores de texto
  foreground: '#f5f5f5',           // Blanco humo para texto principal
  'foreground-light': '#c8c8c8',   // Gris claro para texto secundario
  muted: '#808080',                // Gris medio para texto deshabilitado
  
  // Header/Navbar (negro con detalles rojos)
  'header-bg': '#000000',          // Fondo del header (negro absoluto)
  'header-text': '#f5f5f5',        // Texto del header (blanco humo)
  'header-border': '#dc0000',      // Borde inferior del header (rojo F1)
  
  // Hero "portfolio" text layers (gradiente rojo racing)
  'hero-layer-1': '#1a0000',       // Capa más atrás (rojo muy oscuro)
  'hero-layer-2': '#4d0000',       // Capa trasera media (rojo oscuro)
  'hero-layer-3': '#800000',       // Capa media (rojo medio)
  'hero-layer-4': '#a00000',       // Capa cercana (rojo oscuro vibrante)
  'hero-layer-5': '#dc0000',       // Capa frontal (rojo F1 brillante)
  'hero-year': '#ff1e1e',          // Color del año (rojo brillante)
  
  // Nombre del tema
  name: 'Racing Red F1'
};

export default racingRedTheme;
